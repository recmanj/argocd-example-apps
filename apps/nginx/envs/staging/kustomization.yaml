apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-staging

resources:
- ../../base

patches:
- patch: |-
    - op: replace
      path: /metadata/name
      value: nginx-staging
  target:
    kind: Namespace
    name: kustomize-nginx
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 2
  target:
    kind: Deployment
    name: kustomize-nginx
